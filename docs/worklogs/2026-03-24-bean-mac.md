# 2026-03-24 Work Log
Author: bean-mac

## Today
- 接入飞书手动凭证校验 API，支持凭证校验并写入 HarborBeacon 设置
- 曾增加浏览器辅助配置流程，支持步骤状态跟踪与轮询（该方案后续已移除）
- 曾接入 Playwright 浏览器自动化驱动（该方案后续已移除）
- 在 WebUI 中增加飞书手动校验入口，并曾尝试浏览器辅助入口（后续已删除）
- 将飞书配置流程从“回调地址表单驱动”调整为“浏览器登录 + 控制台内部接口优先”
- 对齐参考仓库的控制台接口调用：机器人启用、事件订阅、权限授予、凭证提取
- 增加相关后端、前端、Playwright 测试
- 本地 Python 测试通过：379 passed
- 真实 Feishu 浏览器验证通过：创建应用、启用机器人、配置事件订阅、授予权限、提取凭证全部成功

## Key Findings
- 飞书当前没有可直接用于“创建应用”的公开 API 可用
- 创建应用仍然必须走浏览器控制台页面
- QR 登录自动检测已打通
- “事件与回调”页面默认不会直接暴露稳定的回调 URL 输入框，继续走 DOM 填表不可靠
- HarborBeacon 更适合对齐 Feishu 长连接模式，事件订阅应优先走 `developers/v1/event/*` 控制台接口
- 真实验证中 `developers/v1/robot/switch/{appId}`、`developers/v1/event/{appId}`、`developers/v1/event/switch/{appId}`、`developers/v1/scope/all/{appId}`、`developers/v1/scope/update/{appId}`、`developers/v1/secret/{appId}` 已证明可用
- `developers/v1/app/create` 仍返回 400，`developers/v1/event/update/{appId}` 仍有参数不合法告警，`developers/v1/scope/list/{appId}` 在新应用上仍可能返回 404

## Current Status
- 当前保留手动填写凭证并校验的链路
- 浏览器辅助配置向导已移除
- Playwright 自动化主流程已移除
- 真实飞书端到端主流程已可跑通并得到有效 `app_id` / `app_secret`
- 事件订阅步骤已从脆弱 DOM 操作切换为长连接模式内部接口路径
- 仍有少量控制台接口 payload / 路径细节待继续收敛，以减少运行日志中的 400/404 告警

## Next
- 把真实浏览器流提取到的 `app_id` / `app_secret` 自动写回 HarborBeacon 设置，完成浏览器验证到配置落库的闭环
- 继续对齐参考仓库，修正 `developers/v1/event/update/{appId}` 的请求体，消除参数不合法告警
- 继续核对 `developers/v1/scope/list/{appId}` 在新应用上的 404 原因，尽量降低权限步骤中的兼容分支
- 评估是否需要把默认事件集合从 `im.message.receive_v1` 扩展到 HarborBeacon 长连接实际使用的最小必需集
- 为真实 Feishu 控制台验证补充更明确的调试工件说明，便于后续复盘 `/tmp/feishu_api_log.json` 和截图
- 已完成：提交并推送本地改动到 GitHub
