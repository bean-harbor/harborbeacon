# 2026-04-18 Codex

## 今日完成

### 1. 清理 Debian 13 部署链路中的旧 Bot 服务残留

- `tools/install_debian13_services.sh` 现在只安装并启用 `assistant-task-api` 与 `agent-hub-admin-api`
- 升级路径中会主动停用并删除遗留的 `feishu-harbor-bot.service`
- 删除已失效的 `tools/run_feishu_harbor_bot.sh`
- `docs/debian13-real-usage-plan.md` 与 `tests/contracts/test_debian_deployment.py` 已同步收口

### 2. 为 HarborBeacon 增加正式 bootstrap 与 bridge runner

- 新增 `harborbeacon/bootstrap.py`
- 新增 `harborbeacon/bridge_runner.py`
- 新增 `tools/run_harborbeacon_bridge.py`
- 新增 `examples/harborbeacon.channels.example.yaml`

当前 HarborBeacon 已具备明确的正式装配层，可统一组装：

- registry
- router
- runtime
- dispatcher
- webhook receiver
- gateway

并在启动时正式接入 `register_camera_domain(...)`。

### 3. 补齐 HarborBeacon 出站 sender，并完成真实 Feishu 收发验证

- 新增 `harborbeacon/senders.py`
- runner 现在优先走真实 sender，失败时才降级到 logging sender
- 已验证真实链路：
  - `Feishu -> HarborBeacon websocket -> assistant-task-api -> Feishu`

这次真实验证不再只是“凭证校验成功”，而是已经确认：

- 入站消息可落到 `camera.scan`
- 任务状态会写入 `.harborbeacon/task-api-conversations.json`
- 出站消息会得到真实 `provider_message_id`

### 4. 废弃并彻底清理不稳定的飞书浏览器自动取密钥方案

- 删除浏览器辅助与 Playwright 相关后端代码
- 删除对应测试
- 前端设置页统一回到“手动填写 `app_id/app_secret`，然后校验并应用”
- 相关历史文档与 worklog 已同步标注该方案已移除

### 5. 补齐 IM Gateway 合同草案并推进到 v1.4

本轮已在仓库中沉淀：

- `HarborBeacon-HarborGate-Agent-Contract-v1.md`
- `HarborBeacon-HarborGate-Agent-Contract-v1.2.md`
- `HarborBeacon-HarborGate-Agent-Contract-v1.4.md`

其中 `v1.4` 重点明确了：

- 入站仍复用 `POST /api/tasks`
- 出站统一为 `POST /api/notifications/deliveries`
- `route_key` 为 Gateway 持有的 opaque 路由句柄
- 同一 IM 消息重试必须复用相同 `task_id`
- `needs_input` 的续执行必须通过 `args.resume_token`
- 通知幂等冲突要比较 `notification_id`
- 路由错误需要纳入统一 HTTP error envelope

## 当前状态

- 本轮 HarborBeacon、部署清理、Feishu 实测链路、IM contract 草案相关改动已同步到 GitHub 分支 `codex/phase-d-sync-2026-04-16`
- HarborBeacon 已经不再依赖旧 `feishu-harbor-bot` 作为正式入口
- Debian 13 部署口径、bridge runner、真实 sender、Feishu 实测链路已经基本对齐
- 飞书配置正式回到“手动填凭证 + 校验”
- IM Gateway 与 HarborBeacon 的跨仓库边界已进入可冻结讨论阶段，等待对方更新到更高版本合同

## IM 协作说明

IM 部分后续明确按分工协作推进，不应继续由单仓库单侧包办。

建议拆分如下：

- 工程师 A：`OpenSource/IM`
  - 各 IM adapter
  - gateway runtime
  - route registry
  - 平台凭据管理
  - 入站归一化与出站投递
- 工程师 B：`HarborBeacon-LocalAgent-Project`
  - `assistant_task_api`
  - 业务状态机
  - approval / artifact / audit
  - `POST /api/tasks` 的 IM 契约收口
  - `POST /api/notifications/deliveries` 的 HarborBeacon 调用侧

协作边界要求：

- 两边只通过 HTTP/JSON contract 通信
- 不互相 import 代码
- 不共享 `.harborbeacon/*.json`
- HarborBeacon 持有业务会话真源
- IM Gateway 持有平台凭据、路由与传输运行态

## 下一步建议

- 等对方更新 `v1.5` 后继续收敛合同冻结版
- 在 HarborBeacon 中补上 `TaskRequest.message` / `source.route_key` / `args.resume_token` 的正式结构定义与回归测试
- 逐步把 HarborBeacon 内部仍直接依赖平台凭据或平台投递的路径迁移到 IM Gateway
- 在两边仓库分别建立共享 fixture / schema 校验，减少后续联调返工

---

## 收工补记（第二轮）

### 6. HarborBeacon 侧已把 v1.5 cutover 主链路基本接通

今天后半段继续把 HarborBeacon 侧的主链路推进到了更接近冻结的状态：

- `POST /api/tasks` 已在 HarborBeacon 内正式接入 `message` block、`source.route_key`、版本头与鉴权校验
- `task_id` 幂等重放与冲突检测已经收口到正式接口层
- `route_key`、`message.message_id`、`trace_id` 已可沿任务、会话、审批、artifact、audit 链路持久化
- HarborBeacon 出站通知已改为走 IM Gateway `POST /api/notifications/deliveries`
- HarborBeacon 管理面已改为消费 IM Gateway redacted status，不再长期持有平台原始凭据

这意味着 HarborBeacon 侧已经从“内部直连平台”的旧形态，切换到了“业务真相留在 HarborBeacon、平台传输留给 IM Gateway”的新边界。

### 7. 协作合同、roadmap 与 plan 已统一到 `harbor-*` lane 口径

今天新增并落地：

- `HarborBeacon-Harbor-Collaboration-Contract-v2.md`
- `HarborBeacon-HarborOS-Control-Collaboration-Contract-v1.md` 的 superseded 说明
- `docs/daily/harbor-daily-sync-template.md`

并同步更新：

- `HarborBeacon-LocalAgent-Roadmap.md`
- `HarborBeacon-LocalAgent-Plan.md`

统一后的协作口径已经明确：

- `harbor-architect` 负责边界治理、cutover、发布与回滚 gate
- `harbor-framework` 负责 HarborBeacon 共享 runtime、北向 contract、task/business state、approval、artifact、audit、本地推理、账号与编排
- `harbor-im-gateway` 负责外部 IM 仓库的 transport/route/delivery/credentials
- `harbor-hos-control` 负责 HarborOS System Domain
- `harbor-aiot` 负责 camera / AIoT / Home Device Domain

### 8. 已为明天的 5 工程师并行推进准备好日终收口方式

今天把“各 lane 自己 push，architect 做日终收口”的规则正式写入协作合同，并新增统一日报模板。

明天开始，推荐的并行工作方式是：

- `harbor-framework`：继续 HarborBeacon 主仓的 framework / contract / task state / approval / audit / local inference 主线
- `harbor-im-gateway`：在 IM 仓库推进 adapter / gateway / route / delivery / credentials
- `harbor-hos-control`：推进 HarborOS System Domain 的 southbound 控制
- `harbor-aiot`：推进 camera / AIoT / Home Device Domain 的 southbound 控制
- `harbor-architect`：每天负责 merge-ready / pending / blocked / rollback 风险收口

## 收工验证

本轮收工前已重新执行：

- `cargo test --lib`
- `cargo test --bin agent-hub-admin-api`
- `cargo test --bin assistant-task-api`

全部通过。

## 收工状态

- 当前 HarborBeacon 侧改动、协作合同、roadmap/plan 更新与每日同步模板准备一起打包
- 本次 GitHub 同步将继续沿用分支 `codex/phase-d-sync-2026-04-16`
- 已推送提交 `c2d35b8 feat(harbor): complete v1.5 cutover groundwork`
- `warp_*.png` 仍为未引用的本地图片产物，暂不纳入本次提交
