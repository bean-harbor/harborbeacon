# Harbor Daily Sync - 2026-05-04

## Lane Sync - harbor-framework - 2026-05-04

### Repo / Branch / PR

- Repo: HarborBeacon
- Branch: `codex/rust-only-harborgate-release-20260501`
- PR: not opened

### Today's Scope

- Closed the Harbor Assistant r40.5 backend loop for model management, hardware-aware recommendations, local model download retry semantics, runtime activation, and camera discovery/delete semantics.

### Completed

- Added hardware readiness fields and model fit/group projections so `.82` recommends lightweight local models and marks Qwen3.5 4B as installed but not recommended for the current hardware.
- Fixed BGE M3 and Qwen2.5 lightweight model download metadata to use Hugging Face/HF mirror repo sources and retry the existing failed job row.
- Kept FlashV4 visible as a disabled cloud fallback endpoint awaiting API key configuration.
- Fixed scan input normalization for `192.168.3.0/554` into `192.168.3.0/24` with RTSP port `554`.
- Preserved r40.4 automatic runtime activation when selecting an installed model.

### Validation

- Ran: `cargo test --bin agent-hub-admin-api`
- Result: passed, 86 tests.
- Prior same-session full lib validation: `cargo test --lib -- --test-threads=1`, passed 370 tests.
- Live `.82`: release `20260504-r405-hardware-model-camera-delete` deployed; hardware profile reported `low_vram_gpu`, memory `11717MB`, GPU VRAM `8192MB`, recommended profile `lightweight-local-models`.

### IM Seam / Rollback Gate

- Inbound task seam status: unchanged
- Outbound delivery seam status: unchanged
- Redacted gateway status available: n-a
- Retrieval launch / handoff pack referenced: n-a
- Explicit `knowledge.search` available: n-a
- NL retrieval fallback canary state: n-a
- Rollback safety status: no rollback blocker introduced
- HarborBeacon direct platform delivery reintroduced: no
- HarborBeacon direct platform delivery count on canary path = 0: yes
- HarborBeacon raw credential ownership or validation reintroduced: no
- HarborOS scope drift into IM transport ownership: no
- Observability fields seen: model download status, model hardware fit, endpoint status, camera credential redaction
- Missing fields or notes: none for r40.5 scope

### GitHub Status

- Commit/push status: local commit `50e83e41`; not pushed
- PR status: not opened

### Cross-Lane Impact

- Touched seam: HarborDesk `/api/harbordesk/*` admin/model/camera surfaces only
- Required collaborators: HarborNAS WebUI for display/interaction closeout
- Frozen boundary affected: no
- IM v1.5 cutover gate package used: n-a

### Risks / Blockers

- Browser was redirected to `/ui/signin`; visual regression behind login was not completed without entering credentials.
- Qwen3.5 4B remains installed/running on `.82` but is now correctly marked as not recommended for this hardware class.

### Rollback Notes

- Roll back HarborBeacon to the previous release bundle if model catalog, download worker, or scan/delete semantics regress.
- Cloud FlashV4 remains disabled unless API key is configured, so rollback does not need to revoke a newly enabled cloud route.

### Next Step

- Start the HarborOS ISO packaging design from the r40.5 release bundle and installer behavior.

## Lane Sync - harbor-aiot - 2026-05-04

### Repo / Branch / PR

- Repo: HarborBeacon
- Branch: `codex/rust-only-harborgate-release-20260501`
- PR: not opened

### Today's Scope

- Closed camera delete semantics and discovery scan behavior for Harbor Assistant r40.5.

### Completed

- Deleting a camera now removes device-specific credentials, evidence, DVR/default references, and no longer preserves username/password/path hints from that deleted device.
- Discovery scan separates scan range from RTSP port and normalizes the user-facing slash-port typo.
- ONVIF-empty discovery falls back to RTSP candidate seeding when RTSP is included in the requested protocols.

### Validation

- Ran: included in `cargo test --bin agent-hub-admin-api`.
- Live `.82`: scan with `192.168.3.0/554` normalized to `192.168.3.0/24` + port `554`, rediscovered `192.168.3.231`, and snapshot proxy returned HTTP 200 `image/jpeg`.

### IM Seam / Rollback Gate

- Inbound task seam status: unchanged
- Outbound delivery seam status: unchanged
- Redacted gateway status available: n-a
- Retrieval launch / handoff pack referenced: n-a
- Explicit `knowledge.search` available: n-a
- NL retrieval fallback canary state: n-a
- Rollback safety status: no new rollback blocker
- HarborBeacon direct platform delivery reintroduced: no
- HarborBeacon direct platform delivery count on canary path = 0: yes
- HarborBeacon raw credential ownership or validation reintroduced: no
- HarborOS scope drift into IM transport ownership: no
- Observability fields seen: credential status redaction, snapshot response, discovery result registration
- Missing fields or notes: none

### GitHub Status

- Commit/push status: included in local commit `50e83e41`; not pushed
- PR status: not opened

### Cross-Lane Impact

- Touched seam: Home Device Domain camera API behavior only
- Required collaborators: HarborNAS WebUI for two-step delete UI
- Frozen boundary affected: no
- IM v1.5 cutover gate package used: n-a

### Risks / Blockers

- Destructive live delete of the real camera was not performed during final closeout; unit/Jest coverage and previous live discovery/snapshot covered the intended semantics.

### Rollback Notes

- Roll back the Beacon r40.5 backend if camera discovery or credential cleanup behavior blocks live camera use.

### Next Step

- Keep camera credential handling explicit during ISO packaging; do not bake camera credentials into the image.

## Lane Sync - harbor-hos-control - 2026-05-04

### Repo / Branch / PR

- Repo: HarborBeacon / HarborNAS-webui deployment bundle
- Branch: same local branches as above
- PR: not opened

### Today's Scope

- Deployed and smoke-tested the r40.5 bundle on HarborOS `.82`.

### Completed

- Built and deployed release bundle `20260504-r405-hardware-model-camera-delete`.
- Verified services active after install and Harbor Assistant assets reachable with `v=r405`.

### Validation

- Ran: live HTTP/API smoke against `.82`.
- Result: WebUI route returned HTTP 200; model API health returned ready; camera snapshot returned JPEG; model catalog/endpoints/downloads reflected r40.5 expectations.

### IM Seam / Rollback Gate

- Inbound task seam status: unchanged
- Outbound delivery seam status: unchanged
- Redacted gateway status available: n-a
- Retrieval launch / handoff pack referenced: n-a
- Explicit `knowledge.search` available: n-a
- NL retrieval fallback canary state: n-a
- Rollback safety status: previous bundle remains the rollback path
- HarborBeacon direct platform delivery reintroduced: no
- HarborBeacon direct platform delivery count on canary path = 0: yes
- HarborBeacon raw credential ownership or validation reintroduced: no
- HarborOS scope drift into IM transport ownership: no
- Observability fields seen: release id, service health, model health, camera snapshot
- Missing fields or notes: browser stayed at sign-in without credential entry

### GitHub Status

- Commit/push status: local commits only; not pushed
- PR status: not opened

### Cross-Lane Impact

- Touched seam: installer/runtime deployment behavior reused; no HarborGate contract change
- Required collaborators: ISO packaging owner next session
- Frozen boundary affected: no
- IM v1.5 cutover gate package used: n-a

### Risks / Blockers

- ISO packaging still needs a clean design for what is baked into the image versus installed/configured after first boot.

### Rollback Notes

- Use the previous HarborOS release bundle if `.82` r40.5 runtime behavior regresses.

### Next Step

- Start ISO packaging from the proven release bundle layout rather than from ad-hoc service copies.

## Lane Sync - harbor-im-gateway - 2026-05-04

### Repo / Branch / PR

- Repo: HarborGate
- Branch: `codex/harborgate-rust-only-20260501`
- PR: not opened

### Today's Scope

- No r40.5 implementation change in HarborGate.

### Completed

- HarborGate was intentionally not touched by the r40.5 model/camera closeout.

### Validation

- Ran: git status only.
- Result: existing dirty file remains `rust/harborgate/src/setup.rs`; not included in r40.5 commits.

### IM Seam / Rollback Gate

- Inbound task seam status: unchanged
- Outbound delivery seam status: unchanged
- Redacted gateway status available: unchanged
- Retrieval launch / handoff pack referenced: n-a
- Explicit `knowledge.search` available: n-a
- NL retrieval fallback canary state: n-a
- Rollback safety status: unchanged
- HarborBeacon direct platform delivery reintroduced: no
- HarborBeacon direct platform delivery count on canary path = 0: yes
- HarborBeacon raw credential ownership or validation reintroduced: no
- HarborOS scope drift into IM transport ownership: no
- Observability fields seen: n-a
- Missing fields or notes: HarborGate dirty diff needs separate review before any commit

### GitHub Status

- Commit/push status: no new commit; not pushed
- PR status: not opened

### Cross-Lane Impact

- Touched seam: none
- Required collaborators: none for r40.5
- Frozen boundary affected: no
- IM v1.5 cutover gate package used: n-a

### Risks / Blockers

- Do not mix the existing HarborGate dirty diff into r40.5 Beacon/WebUI commits.

### Rollback Notes

- n-a for today; no HarborGate runtime change was made.

### Next Step

- Review HarborGate dirty diff separately before ISO packaging decides which Gate artifact enters the image.

## Architect Closeout - 2026-05-04

### Merge-Ready

- HarborBeacon r40.5 backend local commit `50e83e41`.
- HarborNAS WebUI r40.5 local commit `e6641ce6ff`.

### Pending

- Push/PR creation remains pending.
- Browser visual regression behind login remains pending unless credentials are entered by the user or an authenticated session is available.
- HarborOS ISO packaging design and implementation starts next session.

### Blocked

- No current r40.5 code blocker.
- HarborGate dirty diff is intentionally out of scope until separately reviewed.

### Boundary Check

- Frozen contract changed: no
- Cross-lane routing change introduced: no
- New rollback risk introduced: no

### Release / Cutover View

- Safe to merge today: yes for Beacon/WebUI r40.5 local commits after final user review; no for HarborGate dirty diff.
- Safe to cut over today: already deployed to `.82`; broader cutover should wait for browser-authenticated UI review.
- Highest current risk: ISO packaging may accidentally bake runtime state, model cache, or credentials unless first-boot boundaries are designed explicitly.
- Retrieval handoff notes: model-center surfaces changed, but retrieval/IM handoff contracts were not changed.

### Next-Day Order

1. User runs a quick authenticated r405 UI check on `.82`.
2. Push or open PRs for Beacon/WebUI r40.5 if the UI check passes.
3. Start a new session for HarborOS ISO packaging with clean repo status and target registry read first.
