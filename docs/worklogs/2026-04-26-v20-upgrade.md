# 2026-04-26 v2.0 Upgrade Closeout

## Completed

- Preserved the v2.0 control pack as a separate commit.
- Added active `POST /api/turns` handling with `X-Contract-Version: 2.0`.
- Added `TaskTurnEnvelope`, Beacon-owned `conversation.handle`,
  `active_frame`, `continuation`, and top-level `delivery_hints`.
- Removed the active assistant task API `/api/tasks` handler.
- Migrated assistant task API tests from v1.5 task payloads to v2.0 turn
  envelopes.
- Switched active Beacon/Gate defaults to `X-Contract-Version: 2.0` and kept
  v1.5 as historical-only release context.
- Fixed v2.0 clarification feedback so a pending `conversation.clarify` frame
  stays active when the user sends feedback such as `非常好`.
- Fixed v2.0 clip-confirmation feedback with a generalized frame-first policy:
  `camera.clip_confirmation` now persists across no-tool conversation turns
  such as feedback, weather, capability questions, and repair until explicit
  playback, cancel, or a superseding tool intent.
- Built `harbor-release-20260426-v20-clarify-r1.tar.gz` on `.197` and
  deployed it to `.182`.
- Verified `.182` accepts Gateway `X-Contract-Version: 2.0`, rejects `1.5`,
  and passes the direct `/api/turns` live matrix through record/playback with
  `native_video` delivery hints.
- Built `harbor-release-20260426-v20-frame-first-r1.tar.gz` on `.197` and
  deployed it to `.182`.
- Verified the `.182` direct frame-first `/api/turns` matrix:
  `录一段短视频 -> 非常好 -> 回放一下刚刚录的短视频`,
  `录一段 -> 今天天气怎么样 -> 要`, and `录一段 -> 算了`.
- Fixed active-frame preserve rendering so social/affective conversation turns
  are acknowledged before re-anchoring the pending frame.
- Built `harbor-release-20260426-v20-affective-frame-r1.tar.gz` on `.197` and
  deployed it to `.182`.
- Verified `.182` direct `/api/turns` with
  `录一段短视频 -> 非常好 -> 谢谢你 -> 你能干什么 -> 回放一下刚刚录的短视频`;
  affective and capability turns preserve `camera.clip_confirmation` with a
  natural acknowledgement/re-anchor reply, and playback still resolves through
  `native_video`.

## Changed Files

- `src/runtime/task_api.rs`
- `src/bin/assistant_task_api.rs`
- `src/bin/agent_hub_admin_api.rs`
- `src/connectors/im_gateway.rs`
- `src/connectors/notifications.rs`
- `src/scripts/validate.rs`
- `tools/install_harboros_release.sh`
- `tools/release_templates/harborbeacon-agent-hub.env.template`
- `tools/release_templates/bin/harbor-agent-hub-helper`
- `docs/harboros-release-packaging-runbook.md`
- `docs/im-v2.0-cutover-rollback-observability-gates.md`
- `tests/contracts/test_im_v20_control_pack.py`
- `tests/contracts/test_release_packaging_install_lane.py`
- `worklogs/2026-04-26-v20-upgrade.md`

## Tests Run

- `python -m pytest tests/contracts/test_im_v20_control_pack.py -q`
- `python -m pytest tests/contracts/test_im_v20_control_pack.py tests/contracts/test_release_packaging_install_lane.py -q`
- `cargo test --bin assistant-task-api`
- `cargo test general_message_turn_feedback_keeps_active_clarify_frame`
- `cargo test general_message_pending`
- `cargo test clip_confirmation_turn`
- `cargo test handle_general_message_resume_token`
- `cargo test`
- `python -m pytest -q`
- `git diff --check`
- `gh pr checks 3`
- `.182` direct `/api/turns` matrix:
  capability, conversation continue, boundary, clarification,
  clarification feedback, cancel, record clip, and playback.
- `.182` direct frame-first `/api/turns` matrix:
  clip confirmation feedback preserve, boundary preserve, explicit playback,
  affirmative playback, and cancel clear.
- `.182` direct affective-frame `/api/turns` matrix:
  praise acknowledgement preserve, thanks acknowledgement preserve, capability
  answer plus re-anchor, and playback resolve.

## Drift Check

- Beacon v2.0 control-pack guard passed.
- Active assistant task API now requires v2.0 and serves `/api/turns`.
- v1.5 task ingress remains removed from the active assistant task API path.
- Active Beacon and packaged release defaults now use contract `2.0`.
- Active-frame persistence is now a release gate: no-tool conversation acts
  must not clear pending `conversation.clarify` or `camera.clip_confirmation`
  frames.
- Gateway status on `.182` rejects `X-Contract-Version: 1.5`.
- `.182` is now on `HARBOR_RELEASE_VERSION=20260426-v20-frame-first-r1` and
  `IM_AGENT_CONTRACT_VERSION=2.0`; Gateway status accepts `2.0` and rejects
  `1.5`.
- `.182` is now on `HARBOR_RELEASE_VERSION=20260426-v20-affective-frame-r1`;
  Gateway status still accepts `2.0` and rejects `1.5`.
- `git diff --check` reported only Windows line-ending warnings.

## Blockers

- `cargo fmt` could not run because `rustfmt` is not installed for the local
  stable toolchain.
- Real Weixin private-DM evidence still requires a user-side message run.

## Next Exact Step

- Run the user-side Weixin private-DM v2.0 matrix and decide Gate/Beacon merge
  readiness from the recorded task, trace, artifact, and delivery evidence.
